[
  {
    "id": "column.public.order.isPaid",
    "label": "Add column \"isPaid\" to \"order\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "isPaid",
        "table": "order"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"isPaid\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "order",
          "isPaid"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"isPaid\"",
        "sql": "ALTER TABLE \"public\".\"order\" ADD COLUMN \"isPaid\" bool DEFAULT false NOT NULL"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"isPaid\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "order",
          "isPaid"
        ]
      }
    ]
  }
]