[
  {
    "id": "column.public.demand.hoursNeeded",
    "label": "Add column \"hoursNeeded\" to \"demand\"",
    "operationClass": "additive",
    "target": {
      "id": "postgres",
      "details": {
        "schema": "public",
        "objectType": "column",
        "name": "hoursNeeded",
        "table": "demand"
      }
    },
    "precheck": [
      {
        "description": "ensure column \"hoursNeeded\" is missing",
        "sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "demand",
          "hoursNeeded"
        ]
      }
    ],
    "execute": [
      {
        "description": "add column \"hoursNeeded\"",
        "sql": "ALTER TABLE \"public\".\"demand\" ADD COLUMN \"hoursNeeded\" int4"
      }
    ],
    "postcheck": [
      {
        "description": "verify column \"hoursNeeded\" exists",
        "sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\"",
        "params": [
          "public",
          "demand",
          "hoursNeeded"
        ]
      }
    ]
  }
]